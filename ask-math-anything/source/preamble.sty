\usepackage[margin=0.5in]{geometry}
\usepackage{xcolor}
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{inconsolata}
\usepackage{booktabs}
\usepackage{amssymb,amsmath,amsthm}
\newtheorem{definition}{Definition}
\newtheorem*{definition*}{Definition}
\newtheorem{theorem}{Theorem}
\newtheorem*{theorem*}{Theorem}
\usepackage{mathtools}
\usepackage[singlelinecheck=false]{caption}
\usepackage[inline]{enumitem}
\setlist{before=\vspace{\baselineskip},after=\vspace{\baselineskip}}
\renewcommand\labelitemi{}% no bullets
\usepackage{sectsty}% smaller font for \section
    \sectionfont{\fontsize{12}{15}\selectfont}
\setlength\parindent{0pt}% no indents
\usepackage{parskip}
\setlength{\parskip}{0.75\baselineskip plus 2pt}
\usepackage[colorlinks = true,
            linkcolor = black,
            urlcolor  = blue,
            citecolor = black,
            anchorcolor = black]{hyperref}
\usepackage{tikz}
\def\tikzname{Ti\emph{k}Z}
\usetikzlibrary{fit,shapes.geometric}
\usetikzlibrary{backgrounds}% option [framed]
\newcounter{nodemarkers}
\newcommand\circletext[1]{%
    \tikz[overlay,remember picture] 
        \node (marker-\arabic{nodemarkers}-a) at (0,1.5ex) {};%
    #1%
    \tikz[overlay,remember picture]
        \node (marker-\arabic{nodemarkers}-b) at (0,0){};%
    \tikz[overlay,remember picture,inner sep=2pt]
        \node[draw,ellipse,fit=(marker-\arabic{nodemarkers}-a.center) (marker-\arabic{nodemarkers}-b.center)] {};%
    \stepcounter{nodemarkers}%
}%
\usetikzlibrary{calc}
\usepackage{tkz-euclide}
\usepackage[most]{tcolorbox}
\usepackage{empheq}
\newtcbox{\mymath}{%
    nobeforeafter, math upper, tcbox raise base,
    enhanced, colframe=black!30,
    colback=gray!10, boxrule=0.5pt}
\newenvironment{alignbox*}{%
    \empheq[box=\mymath]{align*}}{\endempheq}
\usepackage{adjustbox}
% force top-alignment for lone figures
\makeatletter
\setlength{\@fptop}{0pt}
\makeatother
% mark angles
\usetikzlibrary{quotes,angles}% 
\tikzset{%
  mark angle/.style={%
      every pic quotes/.append style={text=cyan},
      draw=cyan,
      angle radius=1cm,
  }%
}%
\newcommand{\Mod}[1]{\ (\mathrm{mod}\ #1)}
\newenvironment{question}{\vspace{1em}\bfseries}{}%
\iftoggle{showAnswers}{ 
    \newenvironment{answer}{\vspace{1em}\color{blue!90!black}}{}
}{%
    \usepackage{verbatim}
    \newenvironment{answer}{\vspace{0em}\expandafter\comment}{\expandafter\endcomment} 
}%

