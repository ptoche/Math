\documentclass[class=article,border=5pt,tikz]{standalone}

\begin{document}
\begin{tikzpicture}[scale=2, thick, 
    transform shape,
    font=\sffamily\normalsize,
    every node/.style = {draw, circle, minimum size=20mm, inner sep=0mm},
    grow = down,
    level 1/.style={sibling distance=7cm},
    level 2/.style={sibling distance=4cm}, 
    level distance=3cm
  ]%
  \node(O){random day}
   child {node (A){rainy}
     child {node (A1){late}}
     child {node (A2) {not late}}
   }%
   child {node (B) {not rainy}
     child {node (B1) {late}}
     child {node (B2) {not late}}
   };
  \begin{scope}[nodes={draw=none}]
    \path (O) -- (A) node [near start, left]  {$0.8$};
    \path (O) -- (B) node [near start, right] {$0.2$};
    \path (A) -- (A1) node [near start, left]  {$0.45$};
    \path (A) -- (A2) node [near start, right] {$0.55$};
    \path (B) -- (B1) node [near start, left]  {$0.30$};
    \path (B) -- (B2) node [near start, right] {$0.70$};
    \begin{scope}[nodes={below=5mm}]
      \node at (A1) {$0.36$};
      \node at (A2) {$0.44$};
      \node at (B1) {$0.06$};
      \node at (B2) {$0.14$};
    \end{scope}
  \end{scope}
\end{tikzpicture}
\end{document}